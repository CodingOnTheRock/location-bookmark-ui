export class Signup {
    constructor(
        private firstname: string,
        private lastname: string,
        private name: string,
        private email: string,
        private password: string
    ) {}
}
